apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: guacamole-isolation-baseline
  namespace: guacamole
spec:
  # Default-deny ingress with explicit allowlist
  endpointSelector: {}
  ingress:
  # Allow ingress from kGateway (external traffic)
  - fromEndpoints:
    - matchLabels:
        io.kubernetes.pod.namespace: kgateway-system
  # Allow Prometheus scraping
  - fromEndpoints:
    - matchLabels:
        io.kubernetes.pod.namespace: kube-prometheus-system
  # Allow uptime-kuma health checks
  - fromEndpoints:
    - matchLabels:
        io.kubernetes.pod.namespace: uptime-kuma

  egress:
  # Allow intra-namespace communication (guacamole <-> guacd <-> postgresql)
  - toEndpoints:
    - matchLabels:
        io.kubernetes.pod.namespace: guacamole
  # Allow DNS resolution
  - toEndpoints:
    - matchLabels:
        io.kubernetes.pod.namespace: kube-system
        k8s-app: kube-dns
  # Allow external egress for remote desktop connections and OIDC
  - toEntities:
    - world
    - host
  - toCIDR:
    - 0.0.0.0/0
